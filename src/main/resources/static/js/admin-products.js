function showAddForm() {
    alert("Form thêm sản phẩm (popup/modal) sẽ hiển thị ở đây.");
}

// Bạn có thể mở rộng thêm xử lý CRUD, fetch từ server...
document.addEventListener("DOMContentLoaded", function () {
    console.log("Quản lý sản phẩm đã load.");
});
